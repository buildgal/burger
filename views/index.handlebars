<h1>BURGER TOWN</h1>

<h2>Enter your burder information here!</h2>

<ul>
  {{#each Enter Burger}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Enter the burger you want!!!!!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Would you like to devour it?</label><br>
    <input type="radio" name="devour" value="1" checked> Devoured<br>
    <input type="radio" name="devour" value="0"> Returned
  </div>

  <button type="submit">Devour Burger</button>
</form>


